我们看下当前的全部代码:

counter_app_controller.gd
``` python
extends Control

# controller
class_name CounterAppController

# view
@onready var btn_add: Button = $BtnAdd
@onready var btn_sub: Button = $BtnSub
@onready var count_label: Label = $CountLabel

func _ready() -> void:
	# 监听输入
	btn_add.pressed.connect(func(): 
		# 交互逻辑
		CounterApp.count += 1 # +-
		# 表现逻辑
		_update_view()	
	)
	
	# 监听输入 
	btn_sub.pressed.connect(func():
		# 交互逻辑
		CounterApp.count -= 1 # +-
		# 表现逻辑
		_update_view()	
	)

func _update_view():
	count_label.text = "%d" % CounterApp.count # +-

```

counter_app.gd
``` python
class_name CounterAppArchitecture extends Architecture

# model
var count:int = 0

func init():
	pass
```

我们将 count 迁移到了 CounterApp 架构中，这样有没有问题呢？

在小规模项目或者项目前期，可以把一些共享的数据定义在架构上，也就是把架构当做一个静态类或者全局自动加载去使用。

但是当项目规模变大，或者到项目中后期，需要共享的数据会变得很多，这个时候比较好的方式就是将这些变量放在 Model 中管理。

接下来假定我们的引用计数器的 count 因为规模变大原因需要迁移到 model 中，我们看下基本的过程。

## 引入 Model

创建一个 Model 很简单，只需要在 QFramework 面板上（按 ctrl + e 打开)，选择 点击创建 Model 按钮，得到如下界面:

![](https://file.liangxiegame.com/af7f8691-ef2d-4e85-be87-150831a7b1a7.png)

我们先输入名字，叫 CounterApp，如图所示:
![](https://file.liangxiegame.com/0c94cdd7-36c1-46f3-afe5-8c85abadacd7.png)

输入之后，直接可以看到类名和生成路径。

如果我们勾选了 独立文件夹，生成路径就会变成如下:
![](https://file.liangxiegame.com/a836a99e-d78c-49bc-9077-be287005126f.png)
会多创建一个同名的目录。

不过我们不使用这个功能，取消勾选，独立文件夹，如下:
![](https://file.liangxiegame.com/484bd206-9519-44f2-a0ed-9132f822f418.png)

然后点击创建，结果如下:
![](https://file.liangxiegame.com/2c5cc035-bdfc-430a-9df5-815de124ca8b.png)

会自动打开这个 coutner_app_model.gd 脚本，如下:
![](https://file.liangxiegame.com/fc22b60f-d439-469f-a4b8-7d3c7f81a834.png)

创建 Model 之后，需要注册到架构中，打开 CounterAppArchitecture 脚本，代码修改成如下:
``` python
class_name CounterAppArchitecture extends Architecture

# model
var count:int = 0

func init():
	register_model(CounterAppModel.new()) # ++
	pass

```

在 init 函数中注册了 CounterAppModel。

这里我们需要将，counter_app_model.gd 移动到我们的示例文件夹中，如下所示:
![](https://file.liangxiegame.com/acdd67ca-ead3-4a24-936b-5a8ab8be1824.png)

之后删除掉 scripts 目录，我们在这个示例用不到默认的约定。

好了，我们可以先看一下结果，运行当前场景，如下所示:
![](https://file.liangxiegame.com/66a49983-d5eb-43b2-891e-eecb4d6d3abc.png)

没有什么变化，我们查看远程部分，如图所示:
![](https://file.liangxiegame.com/dc975def-ff76-410a-9dcf-0b0bebdf6130.png)

在远程的部分，我们可以清晰地看到 CounterAppModel  已经注册进了 CounterApp 的 Model 中。

与 Unity 版本的 QFramework 不同的是，这次 qfgd 拥抱了 Godot 的 Node，这样的好处是，非常方便调试，可以在游戏运行之后就能看到架构的注册情况，如果选择了 CounterAppModel，我们还能在检查器上看到具体的数据，如图所示:
![](https://file.liangxiegame.com/e2679021-d561-491e-bc76-04ea815694ff.png)

我们取消运行，然后在 CounterAppModel 中创建一个 count 变量，脚本如下:
``` python
class_name CounterAppModel extends AbstractModel

const NAME:String = "CounterAppModel"

func get_model_name() -> String: return NAME

var count:int = 0 # ++

func init():
	pass

```

然后再运行当前场景，可以看到 CounterAppModel 的检查器如下所示:
![](https://file.liangxiegame.com/24871c8e-bb56-4760-b7a7-06b3c9692a59.png)
Count 这个数据就可以直接看到了。

好了，接着把 CounterAppArchitecture 的 count 删掉，代码如下:
``` python
class_name CounterAppArchitecture extends Architecture

func init():
	register_model(CounterAppModel.new())
	pass

```

然后在 CounterAppController 中，引用 count 的部分，全部都通过获得 Model 完成，代码如下:
``` python
extends Control

# controller
class_name CounterAppController

# view
@onready var btn_add: Button = $BtnAdd
@onready var btn_sub: Button = $BtnSub
@onready var count_label: Label = $CountLabel

var _model:CounterAppModel = null

func _ready() -> void:
	# 通过架构获得 model
	_model = CounterApp.get_model(CounterAppModel.NAME) # +-
	
	# 监听输入
	btn_add.pressed.connect(func(): 
		# 交互逻辑
		_model.count += 1 # +-
		# 表现逻辑
		_update_view()	
	)
	
	# 监听输入 
	btn_sub.pressed.connect(func():
		# 交互逻辑
		_model.count -= 1 # +-
		# 表现逻辑
		_update_view()	
	)

func _update_view():
	count_label.text = "%d" % _model.count # +-
	
func _exit_tree() -> void: #+=
	_model = null # ++

```

运行当前场景，结果如下:
![](https://file.liangxiegame.com/d9e03392-cd3f-48ac-abf1-4705ed18a629.png)

当我按下 + 号，结果如下:
![](https://file.liangxiegame.com/956090b8-ea3a-43bb-84c7-36b35b2da519.png)

当我按两下 - 号，结果如下:
![](https://file.liangxiegame.com/ece09191-20da-48f7-b5c7-342363d58d1d.png)

结果没问题。

这样引入 Model 就完成了。

## 什么是需要共享的数据?

之前说过，需要共享的数据放在 Model 中管理，那么什么是需要共享的数据？

常见的总共有三种:
* 需要存储的数据，是时间上共享的数据。即游戏应用关闭之后，下次打开数据是不变的。
* 需要在多个界面或者 Node 上使用的数据，是物理上共享的数据，即游戏不管是打开别的界面还是跳转到别的场景，数据在内存中是不会被释放的。
* 配置表也是共享的数据，即游戏开发阶段所配置的数据，共享给玩家游玩时的引用，提供数据的查询和展示。

那么非共享的数据放在哪里呢？

主要放在 Node 脚本中就行，比如要判断用户是否输入了，如果输入了则显示某个按钮，那么这里就需要一个 bool 变量的判断，这个 bool 变量如果放在 Model 里就不合适了，因为这个 bool 变量只是被当前的界面一次性使用了，除非这个 bool 变量需要存储或者在别的地方引用了，才适合放到 Model 里。

再比如一个常见的问题是，敌人的生命要不要放在 Model 里？

答案是不需要的，敌人的生命交给敌人脚本自己管理即可。

如果敌人的生命需要存储，那么存储和加载就需要将敌人的生命转换成对应的存储用的数据结构，然后将这个数据结构序列化并保存，而 Model 不需要持有这种数据结构。

如果敌人的最大生命需要查询，那么可以从配置表里查询，配置表可以由 Model 管理，也可以由之后要介绍的 System 管理。也可以将最大数据的配置定义在 敌人节点 或者 敌人场景上，把场景当做配置。


## 当前的架构图
![](https://file.liangxiegame.com/53a8179e-6b9f-480c-b58f-8a1f8eb16adb.png)

