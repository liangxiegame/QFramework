我们回顾一下当前的代码，如下:

counter_app_model.gd
``` python
class_name CounterAppModel extends AbstractModel

const NAME:String = "CounterAppModel"

func get_model_name() -> String: return NAME

var count:int = 0 

func init():
	pass

```

counter_app.gd
``` python
class_name CounterAppArchitecture extends Architecture

func init():
	register_model(CounterAppModel.new())

```

counter_app_controller.gd
``` python
extends Control

# controller
class_name CounterAppController

# view
@onready var btn_add: Button = $BtnAdd
@onready var btn_sub: Button = $BtnSub
@onready var count_label: Label = $CountLabel

var _model:CounterAppModel = null

func _ready() -> void:
	# 通过架构获得 model
	_model = CounterApp.get_model(CounterAppModel.NAME) # +-
	
	# 监听输入
	btn_add.pressed.connect(func(): 
		# 交互逻辑
		_model.count += 1 # +-
		# 表现逻辑
		_update_view()	
	)
	
	# 监听输入 
	btn_sub.pressed.connect(func():
		# 交互逻辑
		_model.count -= 1 # +-
		# 表现逻辑
		_update_view()	
	)

func _update_view():
	count_label.text = "%d" % _model.count # +-
	
func _exit_tree() -> void: #+=
	_model = null # ++

```

现在，数据共享的问题，通过引入 Model 解决了。

这里再次强调一下，需要共享的数据放 Model 里，不需要共享的，能不放就不放。

虽然引入了 Model，但是这套代码随着项目规模的发展还是有很多的问题的。

其中最严重也最常见的就是 Controller 会越来越臃肿。

我们简单分析一下为什么 Controller 会越来越臃肿，我们先看下监听用户输入部分的代码，如下:

``` python
# 监听输入
btn_add.pressed.connect(func(): 
	# 交互逻辑
	_model.count += 1 # +-
	# 表现逻辑
	_update_view()	
)

# 监听输入 
btn_sub.pressed.connect(func():
	# 交互逻辑
	_model.count -= 1 # +-
	# 表现逻辑
	_update_view()	
)
```

在处理用户输入的代码中，笔者写了注释，交互逻辑 和 表现逻辑。

交互逻辑 和 表现逻辑在之前有简单介绍过。

交互逻辑，就是从用户输入到数据变更的逻辑。

顺序是 View->Controller->Model。

表现逻辑，就是数据变更到在界面显示的逻辑。

顺序是 Model->Controller->View。

如下图所示:

![image.png](https://file.liangxiegame.com/0b4e1255-ee5d-4223-97a6-2e49cf68715c.png)

虽然交互逻辑和表现逻辑理解起来简单，但是他们非常重要，因为 QFramework 接下来的概念都是围绕这两个概念展开的。

View、Model 以及 Controller 的交互逻辑和表现逻辑形成了一个闭环。构成了完整的 MVC 闭环。

而 Controller 本身之所以会逐渐臃肿，是因为，它负责了两种职责，即改变 Model 数据 的交互逻辑，以及 Model 数据变更之后更新到界面的 表现逻辑。

而在一个有一定规模的项目中，表现逻辑和交互逻辑非常多。一个 Controller 很容易就做到上千行代码。

而大部分的 MVC 方案，解决 Controller 臃肿问题是通过引入 Command 的方式，即引入命令模式，通过命令模式来分担 Controller 的交互逻辑的职责。

QFramework 也是使用了同样的方式解决 Controller 臃肿的问题。

我们来看下用法。

## 引入 Command

打开 QFramework(快捷键 ctrl + e) 面板，然后打开 创建 Command 页面，如下:

![](https://file.liangxiegame.com/3cf2a12a-b1e3-4988-8542-27eb445a6c88.png)

在名字栏输入 IncreaseCount ，结果如下:

![](https://file.liangxiegame.com/8cb55f57-6016-4ca8-a1ee-93c1034009a8.png)

点击创建，结果如下:

![](https://file.liangxiegame.com/b45b862b-c824-4ab0-a2d7-f3f04cb3e55a.png)

此脚本也被自动打开了:

![](https://file.liangxiegame.com/1fedbd5e-1b11-4d3c-8b41-d0c6ad41ac15.png)

我们再用同样的流程，创建 DecreaseCount 命令，按 ctrl + e 打开 QFramework 面板，然后打开 创建 Command 界面，如下:

![](https://file.liangxiegame.com/bc869967-bf11-43a8-b545-07ff87784617.png)

然后在名字栏，输入 DecreaseCount ，点击创建，结果如下:

![](https://file.liangxiegame.com/6afa9dce-0daa-4571-8962-1205da44590b.png)

此时这个脚本被自动打开:

![](https://file.liangxiegame.com/8fc011ad-ccd2-49e0-8cd3-2ebda3300a51.png)

我们将 scripts/commands 移动到示例目录下，然后将示例的目录整理一下，结果如下:

![](https://file.liangxiegame.com/b5092e12-a936-4b35-b0b7-c782a5a78646.png)

models 是这次整理时创建的，这样的目录结构，其实是大部分使用 QFramework 架构的项目的目录结构，以后看到类似的目录结构的项目就知道这个项目使用了 QFramework。

好了，我们来编写脚本，讲 Controller 的交互逻辑迁移到这两个命令里，产生修改的脚本如下:

increase_count_command.gd
``` python
class_name IncreaseCountCommand extends AbstractCommand

func execute():
	var model = get_model(CounterAppModel.NAME) as CounterAppModel # ++
	model.count += 1 # ++
```

decrease_count_command.gd
``` python
class_name DecreaseCountCommand extends AbstractCommand

func execute():
	var model = get_model(CounterAppModel.NAME) as CounterAppModel # ++
	model.count -= 1 # ++
```

counter_app_controller.gd
``` python
extends Control

# controller
class_name CounterAppController

# view
@onready var btn_add: Button = $BtnAdd
@onready var btn_sub: Button = $BtnSub
@onready var count_label: Label = $CountLabel

var _model:CounterAppModel = null

func _ready() -> void:
	# 通过架构获得 model
	_model = CounterApp.get_model(CounterAppModel.NAME) 
	
	# 监听输入
	btn_add.pressed.connect(func(): 
		# 交互逻辑
		CounterApp.send_command(IncreaseCountCommand.new()) # +-
		# 表现逻辑
		_update_view()	
	)
	
	# 监听输入 
	btn_sub.pressed.connect(func():
		# 交互逻辑
		CounterApp.send_command(DecreaseCountCommand.new()) # +-
		# 表现逻辑
		_update_view()	
	)

func _update_view():
	count_label.text = "%d" % _model.count
	
func _exit_tree() -> void:
	_model = null

```

代码很简单，我们用流程图表示如下:

![image.png](https://file.liangxiegame.com/6544f785-0389-46bc-813d-b9c77abdd336.png)

运行当前场景，结果如下:
![](https://file.liangxiegame.com/d9e03392-cd3f-48ac-abf1-4705ed18a629.png)

当我按下 + 号，结果如下:
![](https://file.liangxiegame.com/956090b8-ea3a-43bb-84c7-36b35b2da519.png)

当我按两下 - 号，结果如下:
![](https://file.liangxiegame.com/ece09191-20da-48f7-b5c7-342363d58d1d.png)

结果没问题。

大家可能会问，一个简单的数据加减操作，至于创建一个 Command 对象来承担么？看不出来好处呀，反而代码更多了。

如果整个项目只有一个简单的数据加减操作，那使用 Command 有点多此一举，但是一般的项目的交互逻辑，是非常复杂的，代码量也非常多，这个时候使用 Command 才会发挥作用。

具体发挥什么作用，使用 Command 可以带来很多便利，比如：

* Command 可以复用，Command 也可以调用 Command
- Command 可以比较方便实现撤销功能，如果 App 或者 游戏需要的话
- 如果遵循一定规范，可以实现使用 Command 跑自动化测试
- Command 可以定制 Command 队列，也可以让 Command 按照特定的方式执行
- 一个 Command 也可以封装成一个 Http 或者 TCP 里的一次数据请求
- Command 可以实现 Command 中间件模式
- 等等

OK，通过引入 Command，帮助分担了 Controller 的交互逻辑。使得 Controller 成为一个薄薄的一层，在需要修改 Model 的时候，Controller 只要调用一句简单的 Command 即可。

Command 最明显的好处就是：

- 就算代码再乱，也只是在一个 Command 对象里乱，而不会影响其他的对象。
- 讲方法封装成命令对象，可以实现对命令对象的组织、排序、延时等操作。

更多好处会随着大家的实践慢慢体会到。

当前的 MVC 流程如下：

![](https://file.liangxiegame.com/5ddfe754-110f-4417-8e29-d890e36d4a7a.png)

