有的时候，需要获得当前状态持续的时间，但是只用了 physics_process 生命周期，没有使用 process 生命周期，如果单独为了计时去支持 process 生命周期，一是有点麻烦，二是性能有点浪费。

所以 EasyFSM 支持了由 physics_process 统计的状态持续时间。

我们来看下基本使用。

## 基本使用

在任意场景任意节点，编写如下代码：

``` python
extends Node2D

# 定义状态 id
enum States
{
	Idle
}

# 定义状态机
var fsm:EasyFSM = EasyFSM.new()

func _ready() -> void:
	# 定义 Idle 状态
	fsm.state(
		States.Idle
	).on_physics_process(func(_delta:float):
		print("idle state physics seconds:%f" % fsm.current_physics_seconds)	
	)
	
	# 设置初始态
	fsm.start_state(States.Idle)
	
func _physics_process(delta: float) -> void:
	# 驱动 physics_process 生命周期
	fsm.physics_process(delta)
	
func _exit_tree() -> void:
	fsm.clear()
	fsm = null

```

运行当前场景，结果如下:
![](https://file.liangxiegame.com/313c7107-d64b-425d-be98-511da71bcf0b.png)
结果没问题。