在一些项目开发中，笔者遇到了一种需求，就是几个事件不管哪个触发都执行同样的一段代码。也就是多个事件合并注册的需求。

于是 EasyEvent 提供了 or_event API，用来实现多个事件的合并注册。

# 基本使用

在任意场景任意节点，编写如下代码：

``` python
extends Node2D

class_name EasyEventOrEvent

# 定义事件
var event_a:EasyEvent = EasyEvent.new()
var event_b:EasyEvent = EasyEvent.new()
var event_c:EasyEvent = EasyEvent.new()

func _ready() -> void:
	# 注册事件
	event_a.or_event(event_b).or_event(event_c).register(func():
		print("event a 或 event b:触发")
	).un_register_when_node_exiting_tree(self)


func _process(_delta: float) -> void:
	
	# 按键 1 按下
	if Input.is_key_pressed(KEY_1):
		print("按键 1 按下")
		# 触发事件 A
		event_a.trigger()
		
	# 按键 2 按下
	if Input.is_key_pressed(KEY_2):
		print("按键 2 按下")
		# 触发事件 B
		event_b.trigger()

```

运行之后，不管按下 1 还是 2 都得到同样的输出结果，如下:
![](https://file.liangxiegame.com/4f457f2f-c218-46d7-8593-08952ab633b8.png)

代码很简单。

合并注册同样支持 register_with_a_call 这里就不演示了。

大家可能会问，如果合并注册的某个事件是有参数的怎么办？

答案是注册的时候都用不带参数的函数或者匿名函数注册。