补充一个注意点，注册时调用一次也就是 register_with_a_call 只支持没有参数的事件。

如果是有参数的事件使用 register_with_a_call 会报错，而如果给注册的函数使用了 bind，那么在触发事件时，传什么参数都是 bind 对应的值。

当然，这种需求在笔者开发项目的过程中从来没有遇到过。

而且，所有的使用需求都是，无参数的事件在某些情况下需要使用 register_with_a_call，有参数的事件不需要在初始化的情况下在界面上显示，所以不需要使用 register_with_a_call。